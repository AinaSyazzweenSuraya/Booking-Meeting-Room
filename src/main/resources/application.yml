server:
  port: 8081

spring:
  datasource:
    username: root
    password: mysql@123
    url: jdbc:mysql://localhost:3306/bookingmeetingroom?createDatabaseIfNotExist=true
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQL8Dialect
    open-in-view: false
